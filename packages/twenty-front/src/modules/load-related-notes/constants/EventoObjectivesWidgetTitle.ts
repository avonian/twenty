// The metadata API can't create NOTES-type widgets, so the evento Objetivos
// tab is hosted on a FIELD widget with this exact title; the widget renderer
// intercepts it to render the objectives table instead of the field.
export const EVENTO_OBJECTIVES_WIDGET_TITLE = 'Objetivos';
